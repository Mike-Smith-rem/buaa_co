


// Instantiate the module
and_logic instance_name (
    .op(op), 
    .fun(fun), 
    .addu(addu), 
    .subu(subu), 
    .jr(jr), 
    .beq(beq), 
    .lui(lui), 
    .lw(lw), 
    .ori(ori), 
    .sw(sw), 
    .j(j), 
    .jal(jal)
    );


